# Backend dependencies for Skylens API (FastAPI)
# Pin versions for reproducible builds. UK English comments.

fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.8.2
httpx==0.27.0
cachetools==5.3.3
python-dateutil==2.9.0.post0

# Azure Key Vault (runtime secrets via managed identity)
azure-identity==1.17.1
azure-keyvault-secrets==4.7.0

# Test
pytest==8.2.2

# Azure OpenAI client
openai==1.40.0
